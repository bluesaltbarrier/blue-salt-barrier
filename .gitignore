# Local working files and old artifacts (kept locally, excluded from repo)
GITIGNORE/
WRF_Dockerfiles/

# Simulation output (too large for git)
results_*/
mpas30_*/
*.nc
*.grib2

# Docker data
Docker2/
*.tar.gz
*.tar
# v5 July bundle: ship the Docker image tar out-of-band (too large for git);
# the README and reference scripts in july_v5_bundle/ are tracked.
july_v5_bundle/mpas8-v5-july.tar
# split parts of that image (FAT32 USB transfer) — also out-of-band
july_v5_bundle/mpas8-v5-july.tar.part.*

# Python
__pycache__/
*.pyc
.venv/
.env

Exploratory MPAS Sensitivity Experiments on Rainforest Biogenic Salt Aerosols, Tropical Rainfall*.pdf
# Browser-rendered copies of the manuscript PDF (e.g. "Edge ..." prefix)
*Exploratory MPAS Sensitivity Experiments on Rainforest Biogenic Salt Aerosols*.pdf

# Depletion diagnostic outputs — .txt here; .png re-excluded below after the
# "Allow the final plot images" negation (gitignore: last match wins)
mpas_analysis/depletion_*.txt

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/

# Temporary
*.log
*.tmp
gfs_data/
jan_init/
jan_120km/
run_120km/

# Allow the final plot images
!mpas_analysis/*.png

# ...but still exclude depletion diagnostic PNGs (must come after the negation
# above, since gitignore applies last-match-wins)
mpas_analysis/depletion_*.png

# Preprint PDFs — kept local until submission to EarthArXiv
blue-salt-barrier-preprint-*.pdf

# Claude Code settings (local tooling, not public)
.claude/

# Reusable website revision-cycle hold page (local)
paper-hold.html

# v5 release: internal working material (kept local, not published)
# paper_strategy.md + handoff memo are internal notes
files_transferred/
# paste-helper for the EarthArxiv abstract field
eartharxiv_v5_abstract.txt
# PDF-generation vehicle; the .md is the canonical repo copy
corrigendum_v4_2026-05-23.html

# jan_v2_rerun: exclude local machine artifacts; keep the science scripts
# backup/transfer state files (local)
jan_v2_rerun/backup_state.json
jan_v2_rerun/v2_transfer_state.json
# USB/watchdog utilities (contain local drive letters)
jan_v2_rerun/backup_240km_and_delete.ps1
jan_v2_rerun/backup_to_usb.ps1
jan_v2_rerun/cleanup_local.ps1
jan_v2_rerun/install_watchdog.ps1
jan_v2_rerun/transfer_v2_to_usb.ps1
jan_v2_rerun/watchdog.ps1
# local buggy-data USB-backup workflow doc
jan_v2_rerun/BACKUP_README.md
# manuscript-editing helpers, not reproducibility
jan_v2_rerun/renumber_figures.py
jan_v2_rerun/renumber_for_ens_figure.py
